/**
 * Exam Config Module
 *
 * Loads and parses exam configuration
 * Makes frontend exam-agnostic
 */

export { default as configLoader } from "./configLoader";
export { default as configParser } from "./configParser";
